<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lukelunix.shoppinglistplus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        //To-do List
        <activity
            android:name=".ShoppingListActivity.ShoppingListActivity"
            android:configChanges="keyboardHidden|orientation"
            android:label="Shopping List Plus"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Controller.MainMenuActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.AddItemFromListActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_add_item_from_list"
            android:theme="@style/FullscreenTheme"></activity>
    </application>

</manifest>